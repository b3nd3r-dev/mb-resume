<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Max Bender</title>
  <link rel="stylesheet" href="{{ url_for('static', filename='css/mystyles.css', _external=True)}}" />

</head>

<body>
  <div class="mx-5 my-5">
    <section>
      <div class="columns" style="width: 100%;">
        <div class="column is-5 is-offset-1 ">
          <div class="subtitle has-text-primary is-size-5 has-text-centered">
            This is a programaticaly generated resume, for an update to date & formatted version vist
            <a href="">Bender.io</a>
          </div>
          <div class="title has-text-primary pt-3">Max Bender</div>
          <div class="title is-size-4 has-text-primary">{{aboutme.title}}</div>
          <div class="subtitle has-text-primary" style="margin-bottom:5px;">Email: maxbbender@gmail.com
          </div>
          <br>
          <div class="subtitle has-text-primary">{{aboutme.description}}</div>
        </div>
      </div>
      <div class="columns" style="width: 100%;">
        <div class="column is-10 is-offset-1">
          <hr>
        </div>
      </div>
    </section>
    <section>
      <div class="title has-text-primary has-text-centered is-size-2">Projects</div>
      {% for project in projects %}
      <div class="columns" style="width: 100%;">
        <div class="column is-10 is-offset-1">
          <div class="title has-text-primary">{{project.title}}
            <span class="subtitle is-size-6 has-text-link pl-3">{{project.project_link}}</span>
          </div>
          <div class="subtitle has-text-primary">{{project.link}}</div>
          <div class="info has-text-primary">{{project.long_description | safe}}</div>
        </div>
      </div>
      {% endfor %}
      <div class="columns" style="width: 100%;">
        <div class="column is-10 is-offset-1">
          <hr>
        </div>
      </div>
    </section>
    <section>
      <div class="title has-text-primary has-text-centered is-size-2 pt-2">Experience</div>
      {% for achievement in achievements %}
      <div class="columns" style="width: 100%;">
        <div class="column is-10 is-offset-1">
          <div class="title has-text-primary">{{achievement.name}}</div>
          <div class="subtitle has-text-primary">{{achievement.start_date}} - {{achievement.end_date}} <span
              class="subtitle is-size-6 has-text-link">{{achievement.link}}</span></div>
          <div class="info has-text-primary">{{achievement.desc}}</div>
        </div>
      </div>
      {% endfor %}
    </section>
    <section class="mb-6">
      <div class="columns" style="width: 100%;">
        <div class="column has-text-centered py-5">
          <div class="subtitle has-text-primary ">References available upon request</div>
        </div>

      </div>
    </section>
  </div>

  <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
</body>

</html>